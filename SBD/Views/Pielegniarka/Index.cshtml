@model IEnumerable<SBD.Models.Pielegniarka>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Doswiadczenie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Osoba)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Doswiadczenie)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Osoba.Osobaid)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Pielegniarkaid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Pielegniarkaid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Pielegniarkaid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
