@model IEnumerable<SBD.Models.TypDonacji>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Typ)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Czestotliwosc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Typ)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Czestotliwosc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Typid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Typid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Typid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
